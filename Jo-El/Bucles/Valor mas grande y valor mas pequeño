#creamos la variable num con su int input
num=int(input())
#tambien las variables del minimo y el maximo asignando el num
numMin=num
numMax=num
#creamos un while con True y break si num es igual a 0
while True:
    num=int(input())
    if num==0:
        break
    #si num es menor que numMin se muestra el valor minimo introducido.
    elif num<numMin:
        numMin=num
    #si num es mayor que numMax se muestra el valor maximo introducido.
    if num>numMax:
        numMax=num
#hacemos un print con el num max y el num min solo con las variables, coma y un espacio para que solo se muestre el espacio.
print(numMax, numMin)
    