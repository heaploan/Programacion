#creamos el for con la variable nu y se pone el rango del 1 que es donde empezara a contar.
#el 101 que haria el 100 el numero maximo
#el numero 2
for nu in range (1,101,2):
    #ponemos nu%7 para no mostrar los numeros divisibles por 7 y revisamos que no sea igual a 0 con el 1=
    if nu % 7 != 0:
        print(nu) 
