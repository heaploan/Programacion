from interpreter import *

check = True

while check:
    command = input('').split(',')
    com = command[0].lower()
    if com == 'registrar':
        
    else:
        print("ERROR: comando incorrecto")
