#creamos la variable num1 para poder ponerla en el while
num1=0
#creamos el while con la variable num1 haciendo que entre en un bucle si se pone el 0.
#si se pone un numero mayor a 0 el bucle acaba y hace el for.
while num1 <= 0:
    num1=int(input("Introduce un numero mayor a 0: "))
    #creamos el if para en caso de que se introduzca 0, salte el error e indique que ponga un numero mayor a 0
    if num1<=0:
        print("Por favor, introduce un numero mayor a 0 ")
#creamos el for para poner el rango de los numeros, poniendo el 1 como el mas pequeño
#ponemos el num1+1 como rango maximo, el +1 lo ponemos porque siempre es uno menor ya que empieza a contarse desde 0
for nu in range (1, num1+1):
    #este if es para mostrar los divisores del numero ingresado, siempre y cuando el residuo de num1/nu siempre sea 0 sera divisor del numero ingresado.
    if num1 % nu == 0:
        print(nu)
